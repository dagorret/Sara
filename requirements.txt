# Analytical engine
duckdb>=0.9.2

# Parquet / Arrow support
pyarrow>=14.0.0

# Data handling (limited use: previews, small tables)
pandas>=2.0.0

# Database (metadata)
psycopg2-binary>=2.9.9

# Web framework (admin + API)
Django>=4.2

# CLI
typer>=0.9.0

# Optional but recommended
python-dotenv>=1.0.0

# Runtime: requires Python >=3.10
